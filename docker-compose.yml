# Xóa dòng 'version' ở trên cùng
services:
  robot-test:
    build: .
    volumes:
      - ./results:/app/results
    # Pass environment variables from an .env file into the container
    # This lets the container see BASE_URL when you run with --env-file staging.env
    environment:
      - BASE_URL=${BASE_URL}