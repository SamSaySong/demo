# version: '3.8'

services:
  # Cấp 2: Tên dịch vụ phải thụt vào 2 khoảng trắng so với 'services:'
  robot-test:
    # Cấp 3: Các tùy chọn phải thụt vào 2 khoảng trắng so với 'robot-test:'
    build: .
    volumes:
      # Phải có dấu gạch ngang '-' trước phần volume
      - ./results:/app/results
